import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'orbbit_smart_door',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
